{% extends 'base.html.twig' %}
{% block content %}
    <p>Merci, votre livre est bien enregistré :</p>
    <ul>
        <li>{{ book.title }}</li>
        <li>{{ book.style }}</li>
        <li>{% if book.inStock  %}
            Disponible
            {% else %}
            En rupture
            {% endif %}
            </li>
        <li>{{ book.nbpages }}</li>
    </ul>
{% endblock %}